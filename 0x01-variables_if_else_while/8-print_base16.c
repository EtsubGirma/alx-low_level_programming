#include <stdio.h>
/**
 * main - number 8
 *
 * Return: Always 0
 */
int main(void)
{
	int i;
	int n;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	for (n = 'a'; n <= 'f'; n++)
	{
		{
			putchar(n);
		}
	}
	putchar('\n');
return (0);
}
